# 第十二课：Soft Sibling 面试逐字稿（校招后端）

> 用法：先把第 1 节念顺（目标 5 分钟，别超 6 分钟），再把第 2 节的追问当卡片抽背。
> 术语见 [CONTEXT.md](../CONTEXT.md)；取舍见 [ADR 0001](../docs/adr/0001-soft-sibling-instead-of-changing-uk.md)、[ADR 0002](../docs/adr/0002-lock-fail-abort-deposit-create.md)。

**三条铁律**（说错就翻车）：

1. Soft 身份用系统 `coin`（`usdt_sol`），精确身份用品种 `varietyCode`（`usdt`）。
2. 「同一链上事实最多一张有效单」是**目标**，库只对含 index 的精确键唯一。
3. 锁失败返回空 = 本轮不入账，不是「已经防住了」。

---

## 1. 主线独白（约 5 分钟）

### ① 问题（约 1 分钟）

「我在实习里做的是链上充币的重复建单防护。

背景是：同一笔链上转账，系统有两条发现路径——一条是托管渠道的 webhook 回调，一条是我们自己的扫链服务推 Kafka。两边都会告诉系统『这个地址到账了』。

问题出在身份上。我们充币单的精确唯一键里包含链上 output 的位置，也就是 vout index。但两条路径对这个 index 的解释不一致：有的给 0，有的给链上解析出来的值，历史上甚至出现过负数。键里含了这个会漂移的字段，同一笔链上事实就会被认成两笔——精确键查不到，就再建一张充币单，后面可能重复通知、重复入账。

这是资金问题。我没有去改扫链或渠道怎么编号，而是在建单前加一层闸门：默认 index 不可跨系统对齐，用忽略 index 的业务身份挡住双建单。」

### ② 建单闸门（约 2.5 分钟）

「所有用户充币建单都走同一个入口方法，叫 `resolveOrCreate`。

它的逻辑是：先按用户、系统币种、交易哈希、收款地址抢一把 Redis 分布式锁，注意锁键里**不含** index——因为 index 正是不可信的那个字段。

拿到锁以后在临界区里查两次：第一次用精确身份，也就是含 index 的那套键；查不到再用一套忽略 index 的身份——同用户、同商户、同系统币种、同交易哈希、同收款地址、同链上金额。我们内部把它叫 Soft Sibling，就是业务上的同胞订单。命中就直接复用已有那张单，不执行建单回调；如果命中的那张单 index 和这次不一样，会打一条告警，让运营看到渠道的 index 在漂。

金额比较是在应用层用 BigDecimal 的 compareTo 做的，不是交给数据库等值匹配——不然 1.5 和 1.50 会漏匹配。

补充一句：入账当时有老、新两套引擎在灰度，闸门在两边和扫链路径上都接了，避免只防住一条路。」

### ③ 边界与上线姿势（约 1.5 分钟）

「有三个我觉得比实现更重要的取舍。

第一，锁抢不到怎么办。我选的是返回空、告警、本轮直接不建单，让调用方结束，靠 webhook 或 MQ 重投。因为重复入账要人工追回，晚到账只是等重试——宁可晚到账。

第二，为什么不直接改表的唯一键。精确键已经上线，历史单和对账都按它走；Soft 身份里含金额，也不适合直接做唯一约束。所以这层是补偿，不是硬保证：Soft 开关关掉、币种白名单没命中，或者锁租约丢了，数据库仍然允许两张不同 index 的单存在。这个代价我是清楚的。

第三，因为是补偿层，上线是灰度的：默认关闭，按系统币种白名单放量，还能限定只对灰度测试用户生效。这里有个坑——白名单必须写 `usdt_sol` 这种链上币种，写成 `usdt` 会静默不生效，我专门补了单测卡住。

另外留了一个逃生口：管理端强制补单可以显式跳过 Soft，但仍然要过精确键。因为确实存在同哈希同地址同金额的真两笔，Soft 会误判成同胞，这时需要人工确认后强制建单。」

---

## 2. 追问对答

### Q：为什么不改唯一键，直接按 Soft 身份加 UNIQUE？

「主要是迁移成本和字段性质两点。精确键已经在生产上跑，历史充币单、财务对账、运营补单都按含 index 的这套走，改约束是一次要动历史数据的迁移。另外 Soft 身份里有链上金额，是 decimal，scale 不统一，我们在应用层就是用 compareTo 才躲过 1.5 和 1.50 的问题，直接做唯一约束反而不稳。

所以这版选择不动表，先在建单临界区挡住，配灰度和告警观察渠道 index 到底怎么漂。代价就是它是补偿层：开关关掉或者锁丢了，库层面挡不住。如果要收口，方向是让所有发现路径只信同一套 index，让原来的精确键重新成立——那样这个模块可以下线。」

### Q：有没有从源头把 index 修好，而不是软匹配？

「入口侧确实有过 Circle/Solana 按链上 spl_transfer 解析序号的尝试，让同一条回调路径少写 0。但我这次对外讲的模块是建单闸门：跨路径、并发时解析仍然对不齐，必须在建单临界区忽略 index。源头解析不是这条简历的重点，被问我可以补两句，主线是 Soft。」

### Q：同哈希、同地址、同金额，但链上确实是两笔怎么办？

「Soft 会把它当同胞，复用第一张单，第二笔就可能被吞掉——这是这套匹配已知的误伤方向。

所以有两个东西配套：一是 index 不同的命中一定发告警，让人能分辨到底是渠道漂移还是真的两笔;二是管理端强制补单可以显式跳过 Soft、并指定精确的 index，但仍然要过精确键，所以不会变成随便重复入账的后门。前提是运营先证明链上确实是另一笔资金。」

### Q：为什么锁失败选择不建单？延迟到账用户会投诉。

「因为两种错的代价不对称。晚到账是等一次重试，上游 webhook 和 MQ 本来就会重投；重复入账是钱多给了，要人工去追回，还牵涉对账。所以我选了保守的一侧，同时打告警，让人知道锁在争抢。

如果要改善用户体感，我会往缩短重试间隔、或者按业务补一次主动拉取上游交易的方向做，而不是放宽这个闸门。」

### Q：并发场景你怎么测的？

「单元测试覆盖的是决策路径：开关关闭时只走精确键而且不加锁、命中精确键就不再查 Soft、Soft 命中不执行建单并且告警、强制补单跳过 Soft、锁失败返回空并告警，还有币种白名单大小写和 `usdt` 配错这种情况。锁和查询是 mock 的，验证的是锁内顺序和分支。

我要诚实讲缺口：真实并发下两条路径同时进来只建一张单、以及调用方拿到空之后的重试闭环，我没有做端到端验证，加锁被中断那条分支也没覆盖。所以上线用的是默认关闭加灰度放量，先在小范围看告警和实际订单，而不是靠测试给保证。」

### Q：入账 V1、V2 是什么，和你这块什么关系？

「那是入账引擎在灰度迁移：老一版在回调里同步做风控和到账，新一版走 processor 流水线和异步风控。渠道回调按灰度规则二选一，扫链统一走新版。

和我这块是两件事：引擎版本决定建单之后怎么推进状态，我这层只决定用哪张单。所以闸门在两套引擎和扫链上都接了。有一个细节是：如果扫链撞上渠道已经建好的单，扫链侧只做标记，不去推进状态机，避免两套引擎抢同一张单的状态。」

### Q：是不是所有充币都走这套？

「不是。这套针对的是用户自己的充币地址。还有一类是 Paybis，也就是法币买币的渠道，钱打到专门的地址，走的是另一条建单逻辑，不在这个模块范围里。」

### Q：锁的粒度和参数？

「锁键是用户、系统币种、交易哈希、收款地址拼出来的，和 Soft 身份对齐，故意不含 index——如果把 index 放进锁键，两条路径就会拿到两把不同的锁，等于没锁。

参数上等待是秒级、租约几十秒，够覆盖建单这一小段临界区。极端情况租约过期后临界区失去保护，这时精确键挡不住不同 index，所以我说它是补偿层而不是硬保证。」

### Q：这个模块你觉得有什么不足？

「三点。第一，根因没完全收口——干净做法是所有发现路径共用同一套 index，让原来的精确键成立，代码里还有『同哈希同地址不同 index 怎么办』的 TODO。第二，保证停在应用层，没有落到数据库约束。第三，缺真实并发和重试闭环的验证。

如果给我更多时间，我会先补并发和重试的端到端用例，再评估能不能把身份收敛成可以做唯一约束的字段。」

---

## 3. 上场前 30 秒自检

- 开口第一句说的是「钱会怎么错」，不是「我用了 Redis 锁」。
- 说到币种，嘴上要分开：精确键 `varietyCode`，Soft 和白名单 `coin`。
- 不说「保证不会双入账」，说「目标是最多一张有效单，落地是锁加 Soft，库只保证精确键」。
- 主动交出两件事：不改 UK 的代价、并发测试缺口。
- 主线不讲「解析 index / 两刀」；被问源头再补一句入口解析，立刻拉回闸门。
